class TreeNode:
    def __init__(self, price):
        self.price = price
        self.left = None
        self.right = None
        self.height = 1  
class AVLTree:
    def __init__(self):
        self.root = None
    def insert(self, price):
    def in_order_traversal(self):
        result = []
        def _in_order(node):
            if node:
                _in_order(node.left)
                result.append(node.price)
                _in_order(node.right)
        _in_order(self.root)
        return result
avl_tree = AVLTree()
for price in [299, 199, 499, 99, 399]:
    avl_tree.insert(price)
sorted_prices = avl_tree.in_order_traversal()
print(sorted_prices) 
