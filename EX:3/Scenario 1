class PrintQueue:
    def __init__(self):
        self.queue = []
    def enqueue(self, job):
        self.queue.append(job)
        print(f"Job '{job}' added to the queue.")
    def dequeue(self):
        if not self.queue:
            print("No jobs in the queue.")
            return None
        next_job = self.queue.pop(0)
        print(f"Job '{next_job}' is being processed.")
        return next_job
    def display_queue(self):
        if not self.queue:
            print("Queue is empty.")
        else:
            print("Current print queue:")
            for job in self.queue:
                print(f"- {job}")
print_queue = PrintQueue()
print_queue.enqueue("Document1.pdf")
print_queue.enqueue("Photo.jpg")
print_queue.display_queue()
print_queue.dequeue()
print_queue.display_queue()
